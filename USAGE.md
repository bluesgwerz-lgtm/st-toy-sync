# Toy Sync 完整使用说明

README 是三分钟快速上手，这份是完整手册。装好就能跑的可以不看，出问题再来查。

## 目录

1. [这套东西怎么工作](#1-这套东西怎么工作)
2. [安装（逐步版）](#2-安装逐步版)
3. [首次连接与验证](#3-首次连接与验证)
4. [模式详解](#4-模式详解)
5. [日常使用](#5-日常使用)
6. [提示词定制](#6-提示词定制)
7. [多设备与通道适配](#7-多设备与通道适配)
8. [安全须知](#8-安全须知)
9. [故障排查大全](#9-故障排查大全)
10. [进阶：HTTP 后端](#10-进阶http-后端)

---

## 1. 这套东西怎么工作

```
AI 回复（含隐藏标记 ‹toy:wave›）
   ↓ Toy Sync 扩展在你的浏览器里提取标记
   ↓ WebSocket → ws://127.0.0.1:12345
Intiface Central（你设备上的 App）
   ↓ 蓝牙
你的玩具
```

三个要点：

- **标记你看不见**。AI 按剧情节奏在回复里插 `‹toy:模式›`，扩展默认把它从
  聊天界面隐藏，玩具自己会动。
- **扩展跑在浏览器里，不在酒馆服务器上**。所以云酒馆、合租酒馆、本地酒馆
  都能用——只要「你看酒馆用的这台设备」上装着 Intiface Central。
- **Intiface 负责认设备**。买的什么牌子、什么协议，你和 AI 都不用关心，
  Intiface 的设备库（数百款）来翻译。你的型号是否支持查这里：
  [iostindex.com](https://iostindex.com/)（筛选 Buttplug-C# 列）。

## 2. 安装（逐步版）

### 第一步：装扩展

酒馆 → 顶栏「扩展」（三个方块图标）→ Install extension → 粘贴：

```
https://github.com/bluesgwerz-lgtm/st-toy-sync
```

问装给谁就选当前用户。装完扩展列表出现「🔗 Toy Sync」。
（clone 失败基本是网络问题，需要能访问 GitHub 的环境。）

### 第二步：装 Intiface Central

去 [intiface.com/central](https://intiface.com/central/) 下载对应平台版本：

- **手机玩酒馆** → 装安卓版（商店装不了就下官网 APK）
- **电脑玩酒馆** → 装桌面版，电脑需要蓝牙（没有就买个 USB 蓝牙适配器）

装在哪台设备取决于：**你用哪台设备的浏览器看酒馆，就装哪台**。

打开 App：

1. 点主界面的 **▶ 启动按钮**（Start Server）
2. 进 Devices 页 → 扫描 → 玩具开机 → 出现后点连接
3. 保持 App 在后台运行

### 第三步：教 AI 插标记

把 README 里的 `<toy_sync>` 提示词段落复制进你的预设/系统提示/角色卡，
哪里都行，能进上下文就生效。

## 3. 首次连接与验证

按顺序做，每一步都有明确的成功信号：

| 步骤 | 操作 | 成功信号 |
|------|------|---------|
| 1 | Intiface 启动服务器、连上玩具 | Intiface 设备页显示你的玩具 |
| 2 | 酒馆扩展设置 → Toy Sync → 点「连接」 | 状态变「已连接」，下方出现设备名和通道 |
| 3 | 点「⏹ 全部停止」 | 无报错（顺便记住这个按钮的位置） |
| 4 | 对 AI 说：下条回复带上 ‹toy:wave› | 玩具开始波浪节奏，聊天里看不到标记 |
| 5 | 对 AI 说：停下 | 玩具停止 |

第 4 步动了、第 5 步停了，整条链路就是通的，以后不用再验。

## 4. 模式详解

| 模式 | 曲线 | 典型场景 |
|------|------|---------|
| `wave` | 正弦缓涌，强度中等 | 温和进行段的默认选择 |
| `pulse` | 心跳型双峰脉冲 | 情绪升温、心动时刻 |
| `tease` | 长时间低强度 + 偶发拉满 | 铺垫、欲擒故纵 |
| `edge` | 5 秒爬升 → 0.5 秒满格 → 骤停 | 边缘控制 |
| `deep` | 往复/旋转通道主导，慢而重 | 深重节奏 |
| `devour` | 收缩/吮吸通道主导，密集 | 密集段落 |
| `chaos` | 全通道每 0.3 秒随机 | 失序、崩溃感 |
| `storm` | 全通道恒定满格 | 顶峰（也是功率测试用模式）|
| `gentle` | 各通道低幅慢波 | 事后、抚慰 |
| `climb` | 20 秒 0→满线性爬坡，循环 | 逼近顶点的推进 |
| `stop` | 全停 | 场景结束、切场、任何非亲密内容 |

规则细节：

- 标记格式 `‹toy:模式名›`，半角 `<toy:模式名>` 也认
- 一条回复出现多个标记时，**最后一个生效**
- 未知模式名直接忽略（白名单校验），不会出乱子

## 5. 日常使用

**每次玩之前**（一分钟）：

1. Intiface Central：启动服务器 + 连玩具
2. 打开酒馆，扩展面板确认 Toy Sync 状态是「已连接」
   （没连就点一下「连接」；扩展会自动重连，但 Intiface 重启后首次要手动点）
3. 开始 RP

**玩的时候**：全自动，AI 按剧情切模式。扩展面板的「当前模式」一栏
实时显示在跑什么。

**想停就停**（按可达速度排序）：

1. 在 RP 里直接说「停」「慢一点」——提示词规则 4 强制 AI 立刻发 stop
2. 扩展面板「⏹ 全部停止」
3. 关 Intiface 服务器 / 直接关玩具电源

**玩完收尾**：扩展总开关关掉（自动发一次停止）→ Intiface 断开设备。

**手机使用注意**：保持酒馆标签页在前台。浏览器会对后台标签页限流，
节奏可能冻结——切回来扩展会自动把当前模式从头重启，但后台期间是失控的。
扩展已自动申请屏幕常亮，玩的时候手机不会自动息屏。

## 6. 提示词定制

`<toy_sync>` 段是普通提示词，随便改，改这里不用动扩展：

- **调 AI 的口味**：觉得它太急就在规则里加一句
  `Prefer tease/wave; reserve storm for true peaks`
- **改标记频率**：默认每条回复最多 1 个。想更细腻可以放宽，
  但注意一条回复内只有最后一个标记生效
- **换语言**：规则写中文也行，模式名保持英文小写即可
- **删模式**：不想让 AI 用某个模式，从 Modes 列表里删掉就行

改扩展端白名单（加自定义模式）才需要动代码：`index.js` 顶部
`MODES` 数组和 `PATTERNS` 节奏表，改完自己 fork 一份或提 PR。

## 7. 多设备与通道适配

节奏引擎输出四个抽象通道：**震动 v / 往复 t / 旋转 r / 收缩 s**，
每 0.1~0.5 秒一拍，广播给 Intiface 里连着的**所有设备**。

每台设备按它上报的能力认领通道：

| 设备马达类型 | 认领的通道 |
|-------------|-----------|
| Vibrate（震动，最常见） | v 和 s 取较大值 |
| Oscillate（往复/拍打） | t |
| Rotate（旋转） | r |
| Constrict（收缩/吮吸） | s |
| Linear（活塞类，如飞机杯） | t（换算成行程速度） |

所以：单马达震动玩具能吃到完整节奏曲线（其他通道折算进震动）；
多通道设备各通道各司其职；两人各连各的设备也行——同一个 Intiface
连几台就同步驱动几台。

## 7.5 平台兼容性

| 你的设备 | 能用吗 | 说明 |
|---------|--------|------|
| Android 手机 | ✅ | Intiface 有安卓版，酒馆浏览器和它同机即可 |
| Windows / macOS / Linux 电脑 | ✅ | 需要电脑有蓝牙（或 USB 蓝牙适配器） |
| iPhone / iPad | ❌ 单机不行 | Intiface 无 iOS 版（App Store 政策），且 iOS 浏览器不支持 Web Bluetooth |
| iPhone + 电脑组合 | ⚠️ 有条件 | Intiface 跑电脑上并监听所有网卡，扩展地址填电脑局域网 IP。**仅限 HTTP 访问的酒馆**——HTTPS 云酒馆会以混合内容为由拦截对局域网 IP 的 ws:// 连接（localhost 有豁免，局域网 IP 没有） |

另外注意：「Install extension」是**酒馆服务器**执行 git clone。本地/Termux
部署的酒馆需要该设备能访问 GitHub（国内环境把代理开成全局/TUN 模式再装）；
装不上也可以下载仓库 zip 解压到
`SillyTavern/public/scripts/extensions/third-party/st-toy-sync/` 手动安装。

## 8. 安全须知

- **身体不适直接关玩具电源**。软件停止有三层，但物理开关永远最可靠
- 首次使用先用第 3 节的验证流程在**低强度模式**（wave/gentle）下测试，
  别上来就 storm
- storm/chaos 是全功率模式，确认你和设备都受得了再让 AI 用
- 断连自动保护：浏览器崩溃、标签页关闭时，Intiface 会自动停止所有设备
  （buttplug 协议内置行为），但**手机整机死机不在保护范围**，低概率但知晓
- 提示词规则 4（用户任何减速表达 → 强制 stop）依赖 AI 遵守。绝大多数
  情况可靠，但它是概率系统不是保险丝——真正的保险丝是你手边的停止按钮

## 9. 故障排查大全

**「连接」点了没反应 / 一直连接中**
- Intiface Central 的服务器启动了吗（主界面 ▶）？
- 地址是默认的 `ws://127.0.0.1:12345` 吗？改过 Intiface 端口的话两边要一致
- 酒馆和 Intiface 在同一台设备上吗？跨设备要把地址改成对方局域网 IP，
  且 Intiface 设置里要监听所有网卡（All Interfaces）

**状态「已连接」但设备列表空**
- 先在 Intiface Central 里扫描并连接玩具，扩展这边会自动刷新
- 玩具没被 Intiface 认出来 → 查 [iostindex.com](https://iostindex.com/) 确认支持

**AI 不插标记**
- 提示词贴进去了吗？在生效的预设里吗？
- 直接命令 AI「下条回复带上 ‹toy:wave›」——动了说明链路没问题，
  是 AI 没理解提示词；把 `<toy_sync>` 段挪到更靠前/权重更高的位置

**玩具动了但节奏不对 / 只有恒定震动**
- 看扩展面板设备行的通道标注：设备只有 Vibrate 通道时，
  deep/devour 这类往复、吮吸主导的模式会折算成震动曲线，体感变化小是正常的

**切后台回来没声了**
- 点一下扩展面板「当前模式」旁边的任意操作或重发一条消息即可；
  扩展在回前台时会自动重启节奏，偶尔浏览器杀得太狠需要重连

**报错想反馈**
- 浏览器按 F12 打开控制台，把 `[toy-sync]` 开头的红字截图发 issue

## 10. 进阶：HTTP 后端

不想用 Intiface（自制硬件、或需要厂商私有协议的全部控制粒度）的，
扩展设置里把后端切成「HTTP 模式服务器」，Toy Sync 退化成一个纯标记
转发器：

- 收到标记时：`GET 你的地址/set?mode=模式名`（no-cors，服务器无需配 CORS）
- 「测试连接」按钮会访问：`GET 你的地址/state`，期望返回 `{"mode":"xxx"}`

设备驱动、节奏引擎全部自己实现，模式名语义自定。地址默认
`http://localhost:9090`。

---

*有问题提 [Issue](https://github.com/bluesgwerz-lgtm/st-toy-sync/issues)。玩得开心，注意安全。*
